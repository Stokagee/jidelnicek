"""Pytest fixtures package (test DB, HTTP client, frozen clock, factories).

Imported into `tests/conftest.py`, which re-exports the fixtures so they are
available to every test without an explicit import.
"""
